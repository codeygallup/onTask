{
  "name": "ontask",
  "version": "2.1.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install --include=dev && npm run build",
    "start": "cd server && npm start"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/codeygallup/onTask.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "react": "^19.2.3"
  }
}
